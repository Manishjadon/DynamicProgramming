#include<bits/stdc++.h>  
using namespace std;      
int bf(string &a)  
{   
    int i,adj=10;
    for(i=0;i<a.length();i++)
    {  
        if((a[i]-'0')!=adj)
        {   
            adj=a[i]-'0';   
        }  
        else 
        {  
            break;  
        }  
    }  
    if(i==a.length())
    {  
        return 1;  
    }  
    return 0;  
}   
long int dp[20][11][2][2];
long int solve(string &a,int n,int adj,bool tight,bool lead)
{  
    if(n==0)
    {  
        return 1; 
    }      
    if(dp[n][adj][tight][lead]!=-1)
    {  
        return dp[n][adj][tight][lead];  
    }
    int ub=tight?a[a.length()-n]-'0':9;   
    long int ans=0;  
    for(int i=0;i<=ub;i++)
    {   
        if(i==adj && lead==0)
        {  
            continue;  
        }  
        ans+=solve(a,n-1,i,tight&(i==ub),lead&(i==0));
    } 
    dp[n][adj][tight][lead]=ans;
    return ans;   
}
main()  
{  
    int t=1,i;    
    string a,b;  
    long int ans;
    while(t--)
    {  
        cin>>a>>b;     
        memset(dp,-1,sizeof dp);
        ans=solve(b,b.length(),10,true,true)+bf(a);   
        memset(dp,-1,sizeof dp);
        ans-=solve(a,a.length(),10,true,true);  
        cout<<ans<<"\n";
    }   
}
